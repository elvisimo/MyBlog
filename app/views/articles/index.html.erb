<%= render('header') %>
<p></p>
<p></p>
<p></p>
<p></p> 
<p></p>
<p></p>
<p></p>
<p></p>
 <table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>
 
  <% @articles.each do |article| %>
    <tr>
      <td><%= link_to article.title , article_path(article.id)%></td>
      <td><%= article.text %></td>
      <% if user_signed_in? %>
      <td><%= link_to 'Edit', edit_article_path(article.id) %></td>
      <td><%= link_to 'Delete', article_path(article.id), method: :delete, data: {confirm: 'Are u sure?'} %></td>
      <% end %>
    </tr>
   <% end %>
</table>
<p><%= link_to 'Back', controller:'welcome' %> </p>
<p><%= link_to 'New', controller:'articles', action:'new' %> </p>